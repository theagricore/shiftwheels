import 'package:flutter/material.dart';

class ScreenAddPost extends StatelessWidget {
  const ScreenAddPost({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("screen add post")));
  }
}
